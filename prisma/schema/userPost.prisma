model User_Post {
  id     Int     @id @default(autoincrement())
  name   String
  topic  String?
  tags   Tag[]
  text   String?
  link   String?
  images Image[]
  views  Int?
  likes  Int?

  user   User @relation(fields: [userId], references: [id])
  userId Int
}
