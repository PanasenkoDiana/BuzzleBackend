model Album {
  id     Int      @id @default(autoincrement())
  name   String
  theme  String
  year   DateTime
  images Image[]
  User   User?    @relation(fields: [userId], references: [id])
  userId Int?
}
